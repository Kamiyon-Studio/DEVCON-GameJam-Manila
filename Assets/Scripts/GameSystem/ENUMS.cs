namespace GameSystem {
    public enum GameState {
        WAITING,
        COUNTDOWN,
        PLAYING,
        PAUSED,
        GAMEOVER
    }

    public enum Gravity {
        UP,
        DOWN,
        LEFT,
        RIGHT
    }
}